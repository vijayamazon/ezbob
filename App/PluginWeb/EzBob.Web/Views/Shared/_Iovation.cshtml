@model dynamic

<script type="text/javascript" >
    // set common configuration variables
    var io_install_stm = false; // do not install Active X 
    var io_exclude_stm = 12; // do not run Active X 
    var io_install_flash = false; // do not install Flash 
    var io_enable_rip = true; // collect Real IP information 
    var io_last_error = "";// string This variable contains the last known error

    var io_subscriber_code = @ConfigManager.CurrentValues.Instance.IovationSubscriberId.Value;
    
    //Subscriber supplied variables for snare// Snare operation to perform
    var io_operation = 'ioBegin';

    // io_bbout_element_id should refer to the hidden field in your form that contains the blackbox
    //var io_bbout_element_id          = 'ioBlackbox';

   //  use callback to process blackbox as soon as it is done 
    //var io_bb_callback = function (bb, isComplete) {
    //    // when done call the send function 
    //    if (isComplete) EzBob.App.Iovation.io_send_bb(bb);
    //};
</script>

<script type="text/javascript" src="@ConfigManager.CurrentValues.Instance.IovationUrl.Value"></script>
