@model dynamic
<div class="dashboard-steps">
	<ul class="application_steps">
		<% _.each(steps, function(step, i) { %>
		<li data-step-num="<%= i %>" class="<%= step.css %>">
			<span class="step_label">Step <%= i + 1 %>:
			</span>
			<%= step.title %>
		</li>
		<% }) %>
	</ul>
</div>
