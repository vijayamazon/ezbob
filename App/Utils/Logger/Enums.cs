namespace Ezbob.Logger {
	#region enum Severity

	public enum Severity {
		Debug,
		Info,
		Warn,
		Error,
		Fatal
	} // enum Severity

	#endregion enum Severity
} // namespace Ezbob.Logger