namespace AutomationCalculator.ProcessHistory {
	// Enum int values are ids in the DB, don't change them.
	public enum ProcessCheckpoints {
		Creation      = 1,
		Initializtion = 2,
		GatherData    = 3,
		RunCheck      = 4,
		MakeDecision  = 5,
		OldWayFlow    = 6,
	} // enum ProcessCheckpoints
} // namespace
