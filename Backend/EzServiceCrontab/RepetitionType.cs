namespace EzServiceCrontab {
	/// <summary>
	/// Do not change numeric values - they are used as keys in DB.
	/// </summary>
	enum RepetitionType {
		Monthly = 1,
		Daily = 2,
		EveryXMinutes = 3,
	} // enum RepetitionType
} // namespace
