namespace Integration.ChannelGrabberConfig {
	#region enum Behaviour

	public enum Behaviour {
		Default,
		HMRC,
	} // enum Behaviour

	#endregion enum Behaviour
} // namespace Integration.ChannelGrabberConfig
